[package]
name = "pyusp-wheel"
version = "0.2.0"
edition = "2021"
publish = false

[lib]
name = "_pyusp"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["abi3-py39", "extension-module"] }
pyusp = { path = "../.." }
